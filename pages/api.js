import {baseUrl} from './constant'

export const api = {
  WXLogin: `${baseUrl}/WXLogin?code=`,
  LoadCalendar: `${baseUrl}/LoadCalendar`,
  LoadSubCalendar: `${baseUrl}/LoadSubCalendar`,
  AddCalendar: `${baseUrl}/AddCalendar`,
}
