<view class="form-panel">
  <view class="title">{{ title }}</view>
  <view class="tags">
    <view 
      wx:for="{{ list }}"  
      wx:key="unique"
      class="tag {{item.selected ? 'selected' : ''}}"
      data-id="{{ item.id }}"
      bindtap="onselected"
    >{{ item.val }}</view>
  </view>
</view>
